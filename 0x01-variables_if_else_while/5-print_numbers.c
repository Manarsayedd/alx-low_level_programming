#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: outsput all the alphaabet letters except e and q in lowercase
 *
 * Return: always 0 (success)
 */
int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
{
printf("%i", i);
}
putchar("\n");
return (0);
}
