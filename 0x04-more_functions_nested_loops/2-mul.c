#include "main.h"
#include <stdio.h>
/**
 * mul - multiply 2 numbers
 *
 * description: a function used to multiply 2 numbers
 * @a: first input
 * @: second input
 *
 * Return: always 0 (success)
 */
int mul(int a, int b)
{
	int mutiply;
	multiply = a * b;
	return (multiply);
}
